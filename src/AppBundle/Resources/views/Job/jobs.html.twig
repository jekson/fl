{% extends "AppBundle:Default:base.html.twig" %}

{% block title %}Работа{% endblock %}
{% block head %}
    {{ parent() }}
{% endblock %}

{% block leftmenu %}
<div class="sidebar">
    <div class="block ui_block">
        <ul class="ui">
            <li class="title">Все категории <span class="num">1</span></li>
            {% for category in categoryes %}
                <li><a href="#">{{ category.name }}</a><span class="num">1</span></li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}
{% block body %}
<div class="content">
    <div class="jobs_list">
        {% for job in jobs %}
            {% include 'AppBundle:Job:job_item.html.twig' with {'job': job} %}
        {% endfor %}
    </div>
</div>
{% endblock %}